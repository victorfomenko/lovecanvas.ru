app.controller('deliveryController', function ($scope) {

});
/*
app.controller("loginController", function($scope, $http){
    $scope.submit = function(){
        var data = {
            email:           $scope.email,
            password:        $scope.password

        };
        var ajaxURL = 'ajax/ajax.php?act=login&email=' + data.email + '&password=' + data.password;
        $http.post(ajaxURL).success(function(data){
            console.log(data);
        }).error(function(data){
            console.log(data);
        });
    }
});*/
